apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentinelai.fullname" . }}-config
  labels:
    {{- include "sentinelai.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
