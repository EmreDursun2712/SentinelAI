# Raw datasets are never committed (CIC-IDS2017 etc.).
data/

# Trained artifacts are large binaries reproduced by `python -m ml.train`.
# Ignore everything under artifacts/ except the placeholder that keeps the dir.
artifacts/*
!artifacts/.gitkeep

.ipynb_checkpoints/
.ruff_cache/
.DS_Store
